-- config/devices.lua -- names of the physical devices this config cares about.
return {
  -- NOTE: this uses a Unicode right single quote, not an ASCII apostrophe.
  cameras = { "r35-iPhone17.Pro.Max Camera" },
}
