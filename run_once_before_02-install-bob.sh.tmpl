{{- if or (eq .chezmoi.os "darwin") (eq .chezmoi.os "linux") -}}
#!/bin/bash
# Install bob (neovim version manager) if not already installed
# https://github.com/MordechaiHadad/bob

source "${CHEZMOI_SOURCE_DIR}/.shlibs/sh/logging.sh"
set -e

if command -v bob &>/dev/null; then
    r35_log_ok "bob is already installed: $(bob --version)"
    exit 0
fi

if [ -x "$HOME/.local/bin/bob" ]; then
    r35_log_ok "bob is already installed at ~/.local/bin/bob"
    exit 0
fi

r35_log_header "Installing bob (neovim version manager)..."
curl -fsSL https://raw.githubusercontent.com/MordechaiHadad/bob/master/scripts/install.sh | bash

r35_log_ok "bob installed successfully"
{{ end -}}
