# Generated by scripts/gen_glyphs.lua. Reformatting these is pointless -- the
# generator rewrites them from scratch, so any stylua pass is undone on the next
# `nvim -l scripts/gen_glyphs.lua`. types.lua is also ~21,600 lines, which stylua
# is very slow to walk.
lua/r35/glyphs/data.lua
lua/r35/glyphs/types.lua

# Tooling scratch, not config source.
.aide/
.remember/
